name: rustmail

services:
  rustmail:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "1025:1025"
      - "8025:8025"
    volumes:
      - rustmail-data:/data
    environment:
      RUSTMAIL_LOG_LEVEL: info
    restart: unless-stopped

volumes:
  rustmail-data:
